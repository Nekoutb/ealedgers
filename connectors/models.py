"""Connectors-layer models.

Empty at Step 11. The ERPConnection / ERPOperation models live in the
`accounting` app (Step 7). This app holds connector CODE (clients,
capability registry, sagas), which is mostly non-ORM. Any connector-local
models (e.g. cached capability snapshots) land from Step 28.

See docs/EXECUTION_PLAN.md §2 (L6) and the ERP capability matrix.
"""

# (no models yet — Step 28+)
