"""Ingestion-layer models.

Empty at Step 11. Arrives from Step 52:

  - Document          — an uploaded/emailed source doc (PDF, image) + its
                        OCR-extracted structured data, routed to a department
  - IngestedRecord    — a normalised inbound record (bank line, ERP pull)

See docs/EXECUTION_PLAN.md §2 (L2 Ingestion) and Phase P05.
"""

# (no models yet — Step 52)
