Django>=5.1,<5.2
Pillow>=10.0
python-dateutil>=2.8
gunicorn>=22.0
# PostgreSQL driver (Phase 0.3). On Windows dev hosts without libpq, use
# `pip install "psycopg[binary]"` which ships a self-contained wheel.
# Optional: if DATABASE_URL is not set, the app continues to run on SQLite.
psycopg[binary]>=3.1
